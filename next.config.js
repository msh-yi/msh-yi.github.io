// next.config.js
/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
    // Enable static HTML export on `next build`
    output: 'export',
  
    // Optional: if you need clean URLs with trailing slashes
    // trailingSlash: true,
  
    // Optional: customize the export directory
    // distDir: 'dist',
  };
  
  module.exports = nextConfig;
  